<footer class="footer">
  <div class="container footer__inner">
    <p class="footer__disclaimer">No fund management. Real volume only.</p>
    <div class="footer__bottom">
      <span class="footer__copy">&copy; Viraliza</span>
      <nav class="footer__links" aria-label="Footer navigation">
        <a href="#contact" class="footer__link">Contact</a>
        <a
          href="https://x.com/ViralizaIo"
          class="footer__link"
          target="_blank"
          rel="noopener noreferrer"
        >
          X
        </a>
      </nav>
    </div>
  </div>
</footer>

<style>
  .footer {
    padding-block: 2.5rem 3rem;
    border-top: 1px solid var(--border);
  }

  .footer__inner {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .footer__disclaimer {
    font-size: 0.8125rem;
    color: var(--text-dim);
    letter-spacing: 0.02em;
  }

  .footer__bottom {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .footer__copy {
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--text-dim);
  }

  .footer__links {
    display: flex;
    gap: 1.5rem;
  }

  .footer__link {
    font-size: 0.875rem;
    color: var(--text-dim);
    transition: color 0.2s;
  }

  .footer__link:hover {
    color: var(--text);
  }
</style>
